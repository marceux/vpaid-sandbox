import React from 'react';
import { shallow, mount } from 'enzyme';
import VideoWrapper from './VideoWrapper';

describe('<VideoWrapper />', () => {
  let component;

  beforeAll(() => {
    component = mount(<VideoWrapper />);
  });

  it('', () => {});
});
